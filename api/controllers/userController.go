package controllers

import "github.com/gin-gonic/gin"

func CreateUser(c *gin.Context) {
	// take the request body and unmarshal it into a struct

	// validate the struct

	// create a new user in the database

	// catch any error when creating the user

	// return the user as a JSON response
}
